## Streams shortcut

#### Arrays Streams
```
int[] sortedArray = Arrays.stream(practicingSorting).sorted().toArray();

What does it do?
Converts array back to array, because it returns array it must
be assigned to an array.
```