## Inclusive And Exclusive

```textmate
Inclusive means the boundary value itself evaluates to true (uses ≤ or ≥).

Exclusive means the boundary value is skipped or evaluated to false (uses < or >).
```